<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.LibroSolidario" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/brand_background</item>
        <item name="android:navigationBarColor">@color/brand_background</item>
    </style>

    <style name="Theme.LibroSolidario" parent="Base.Theme.LibroSolidario" />

    <style name="Theme.LibroSolidario.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.LibroSolidario</item>
    </style>
</resources>